# MIDI Generative Delay Web App 仕様書

## 1. プロジェクト概要
Web MIDI APIを使用し、仮想MIDIデバイスから入力されたノート信号を特定の確率ルールに基づいて別のノートに変換し、指定されたBPMの8分音符分遅延させて出力MIDIデバイスへ送信するWebアプリケーション。

## 2. 機能要件

### 2.1 MIDI入出力
- Web MIDI APIを使用して、システム上で利用可能なMIDI入力・出力デバイスを取得する。
- ユーザーがUIから任意の入力デバイスと出力デバイスを選択できる。
- 選択された入力デバイスからの `Note On` および `Note Off` メッセージをリッスンする。
- 変換・遅延処理を行った後、選択された出力デバイスへ `Note On` および `Note Off` メッセージを送信する。
- **和音（ポリフォニック）対応**: 複数のノートが同時に入力された場合でも、各ノートを独立して処理し、すべてメモリ（キュー）に追加する。

### 2.2 出力タイミングと音価（Duration）の処理
- **遅延時間**: 指定されたBPMにおける「8分音符」分の時間だけ、入力を遅延させて出力する。
  - 計算式: `DelayTime(ms) = (60000 / BPM) / 2`
- **音価**: 入力時と同じ長さを維持する。
  - `Note On` を受信したタイミングから `DelayTime` 後に変換後のノートで `Note On` を出力。
  - `Note Off` を受信したタイミングから `DelayTime` 後に同じノートで `Note Off` を出力する。
- 音楽的なタイミングを正確に保つため、JavaScriptの `setTimeout` ではなく、Web Audio APIを用いた精度の高いスケジューリングで実装すること。

### 2.3 メモリーとスケール分析
- 入力されたノートを最新から最大32音まで配列（メモリ）として保持する（和音入力時もそれぞれの音を個別にメモリに追加）。
- メモリ内のノートデータから「現在のスケール」を分析・判定する。
- メモリが更新されるたびに、UIのノートリストと現在のスケール表示を更新する。

### 2.4 出力ノートの制限（オクターブと範囲）
- ユーザーがUIで設定した出力ノート範囲（デフォルトは C2 〜 C6）内に収まるように出力ノートのオクターブを調整する。
- 算出された音階（例: G）に対し、入力音のオクターブに最も近く、かつ指定された範囲内に収まるオクターブを選択する。

### 2.5 ノート変換ロジック
入力されたノートに対し、これまでの入力音数（1〜8音目か、9音目以降か）に応じて以下の確率ルールを適用し、出力するノートナンバーを決定する。

※「スケールと全く関係のない音」とは、1オクターブ12音のうち、対象となる7音のスケールに含まれない残り5音の中からランダムに選ばれた音を指す。

#### A. 初回フェーズ（1音目 〜 8音目まで）
入力された音をRootとし、UIで選択されたスケール（メジャーまたはマイナー）を基準に、以下の確率で出力ノートを決定する。
- **50%**: トニック（第1音 / Root）
- **30%**: サブドミナント（第4音）
- **10%**: ドミナント（第5音）
- **5%**: 第2音
- **4%**: 第6音
- **1%**: スケールと全く関係のない音

#### B. 定常フェーズ（9音目以降）
スケール分析のためのデータが溜まった9音目以降は、以下の2つのルール群からそれぞれ50%ずつの割合で最終的な確率を合成して出力ノートを決定する。

**ルール1（入力音ベース：全体の50%の確率で適用）**
初回のルールをそのまま半分の確率で適用。UIで選択されたスケール（メジャー/マイナー）を利用する。
- 25.0%: 入力音をRootとしたトニック
- 15.0%: 入力音をRootとしたサブドミナント
- 5.0%: 入力音をRootとしたドミナント
- 2.5%: 入力音をRootとした第2音
- 2.0%: 入力音をRootとした第6音
- 0.5%: 入力音をRootとしたスケール外の音

**ルール2（分析スケールベース：全体の50%の確率で適用）**
過去32音から分析された「現在のスケール（Rootとメジャー/マイナー）」を基準とする。
- 25.0%: 現在のスケールのトニック
- 15.0%: 現在のスケールのサブドミナント
- 5.0%: 現在のスケールのドミナント
- 5.0%: 現在のスケールと全く関係のない音

## 3. UI（ユーザーインターフェース）仕様
画面上に以下のコントロールと情報表示パネルを配置する。デザインはモダンで直感的なものとする。

1. **MIDIデバイス設定**
   - 入力MIDIデバイス選択: プルダウンメニュー
   - 出力MIDIデバイス選択: プルダウンメニュー
2. **ジェネレーティブ設定**
   - BPM設定: 数値入力フィールド（ユーザーが直接変更可能）
   - ベーススケール設定: 「Major」または「Minor」の選択ドロップダウン（デフォルト: Major）
   - 出力音域設定（Min/Max）: ドロップダウン等で範囲を指定（デフォルト: Min=C2, Max=C6）
3. **ステータス表示**
   - 現在のスケール表示: 分析結果のテキスト表示（例: "C Major", "A Minor", "Analyzing..." など）
   - 入力ノートカウント: 現在何音入力されたか（1〜8音目か、9音目以降か分かるように）
4. **記憶ノートリスト**
   - 最大32音の入力履歴を表示するリスト。音名（C4, D#4 など）で表示し、最新のノートが一番上にくるようにする。

## 4. 技術スタック・実装上の注意点
- HTML / CSS / Vanilla JavaScript (モジュール分割推奨) 
- Web MIDI API
- クライアントサイドのみで完結すること（サーバーサイドの処理は不要）。
- Web MIDI APIはブラウザのセキュリティ制限があるため、ユーザーのアクション（「Start」ボタンのクリックなど）をトリガーにしてAudioContextやMIDIアクセスを初期化する設計にすること。